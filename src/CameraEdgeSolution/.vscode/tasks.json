{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "buildCameraModule",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/modules/CameraModule/CameraModule.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}